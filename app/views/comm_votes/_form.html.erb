<%= form_for(@comm_vote) do |f| %>
  <% if @comm_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comm_vote.errors.count, "error") %> prohibited this comm_vote from being saved:</h2>

      <ul>
      <% @comm_vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :vote_id %><br />
    <%= f.number_field :vote_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
