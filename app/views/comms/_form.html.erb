<%= form_for(@comm) do |f| %>
  <% if @comm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comm.errors.count, "error") %> prohibited this comm from being saved:</h2>

      <ul>
      <% @comm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
